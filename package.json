{
  "name": "shelter-match-app",
  "version": "1.0.0",
  "description": "A platform to match homeless individuals with available shelter beds and job opportunities",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "keywords": [
    "shelter",
    "homeless",
    "matching",
    "social-impact"
  ],
  "author": "Congressional App Challenge",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.12.2"
  }
}
